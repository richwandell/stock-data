version: '2'
services:
  flask:
    image: 'richwandell/python3-ml:second'
    ports:
      - "5000:5000"
    tty: true
    volumes:
      - './:/app'
    working_dir: /app
    command: tail -f /var/log/app.log

